# WebTab
WebTab — a custom browser new tab page with actual main character energy. Cursor-following spider, a hero swinging across the screen, live quote API, and a search bar with a spider hanging off it. Pure HTML/CSS/JS, no frameworks, no templates, chose violence on the default new tab.
